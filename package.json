{
  "name": "trello-webhook-client",
  "version": "1.0.0",
  "description": "Basic Trello webhook client to dump Trello webhook requests",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=development node ./index.js",
    "test": "NODE_ENV=test ./node_modules/.bin/mocha --expose-gc --async-only --check-leaks --trace-deprecation --reporter spec --no-exit --recursive --ui tdd test/"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "log4js": "^6.3.0",
    "marked": "^3.0.8"
  },
  "devDependencies": {
    "mocha": "^9.1.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tiblu/trello-webhook-client.git"
  },
  "keywords": [
    "trello",
    "webhooks",
    "express"
  ],
  "author": "tiblu",
  "license": "Apache 2.0",
  "bugs": {
    "url": "https://github.com/tiblu/trello-webhook-client/issues"
  },
  "homepage": "https://github.com/tiblu/trello-webhook-client#readme"
}
